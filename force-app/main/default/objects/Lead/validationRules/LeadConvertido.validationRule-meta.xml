<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>LeadConvertido</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
    IsConverted,
    ISCHANGED(Status),
    ISPICKVAL(Status, &apos;Lead Score&apos;),
    OR(
        ISBLANK(WhatsYourInstagram__c),
        ISPICKVAL(Segmento__c, &apos;&apos;),
        ISBLANK(Company),
								ISBLAnK(City),
								ISBLANK(State)
    )
)</errorConditionFormula>
    <errorMessage>Preencha todos os campos da seção &quot;Conversão&quot; antes de converter o Lead.</errorMessage>
</ValidationRule>
